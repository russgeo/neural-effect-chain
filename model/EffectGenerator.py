import EffectDecoder, ParameterDecoder 

def generate_sequence(max_seq_len,effect_decoder, parameter_decoder, input, target,preprocessor, idx_to_effect):
    '''
    function used to generate a sequence of effects
    '''
    effects_sequence = []
    parameters_sequence = []
    while len(effects_sequence) < max_seq_len:
        pass
        # preprocess audio

        # pass into effect decoder

        # pass into parameter decoder

        # process audio with predicted effects and parameters

    #

    return